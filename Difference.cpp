#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout << fixed << setprecision(2);
    long long a,b,c,d;

    cin >>a>>b>>c>>d;

    cout<<"Difference = "<<(a*b) - (c*d);
    cout << "\n";

    return 0;
}
